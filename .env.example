# Gumroad product id for Job Search OS (permalink: aijqck).
# Required for products created on/after 2023-01-09 — find it in Gumroad
# Products → Job Search OS → URL or product settings (numeric / UUID id).
# The app sends BOTH product_permalink=aijqck AND product_id when set.
VITE_GUMROAD_PRODUCT_ID=fJan0IaWHV6TDv7OEAb6EA==

# Dev only — skips the license gate. Never set this in production / release builds.
# VITE_DEV_SKIP_LICENSE=1
